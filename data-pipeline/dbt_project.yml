name: weather_transform
version: 0.1.0
config-version: 2

profile: weather_transform

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  weather_transform:
    marts:
      +materialized: table
